import {UpdateFiscaliaHandler} from './update-fiscalia-handler';

describe('UpdateFiscaliaHandler', () => {
  it('should be defined', () => {
    expect(new UpdateFiscaliaHandler()).toBeDefined();
  });
});
