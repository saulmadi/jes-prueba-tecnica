import {CreateFiscaliaValidator} from './create-fiscalia-validator';

describe('CreateFiscaliaValidator', () => {
  it('should be defined', () => {
    expect(new CreateFiscaliaValidator()).toBeDefined();
  });
});
