import {UpdateExistanceValidator} from './update-existance-validator';

describe('UpdateExistanceValidator', () => {
  it('should be defined', () => {
    expect(new UpdateExistanceValidator()).toBeDefined();
  });
});
