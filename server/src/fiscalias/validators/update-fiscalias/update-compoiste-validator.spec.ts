import {UpdateFiscaliaValidator} from './update-fiscalia-validator';

describe('UpdateCompoisteValidator', () => {
  it('should be defined', () => {
    expect(new UpdateFiscaliaValidator()).toBeDefined();
  });
});
